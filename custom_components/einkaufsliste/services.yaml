add_item:
  fields:
    name:
      required: true
      example: "Milch"
      selector:
        text:
    store:
      example: "Aldi"
      selector:
        text:
    category:
      example: "Kühlregal & Milch"
      selector:
        text:
    quantity:
      example: "2 Liter"
      selector:
        text:
    added_by:
      example: "Oma"
      selector:
        text:

check_item:
  fields:
    name:
      required: true
      example: "Milch"
      selector:
        text:
    store:
      example: "Aldi"
      selector:
        text:

remove_item:
  fields:
    name:
      required: true
      example: "Milch"
      selector:
        text:
    store:
      example: "Aldi"
      selector:
        text:

clear_checked:

cleanup:
  fields:
    force:
      default: false
      selector:
        boolean:
    min_age_days:
      example: 7
      selector:
        number:
          min: 0
          max: 365
          mode: box
